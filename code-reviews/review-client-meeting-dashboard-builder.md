# Independent Review — Client Meeting Dashboard Builder Package

- Run: `6a579236e2f0`, step `independent_package_review`, attempt 1
- Subject: `artifacts/current/client-meeting-dashboard-builder/` (6 parts), materialized from the digest-pinned DataForge handoff for source run `31ad67eea2f1`
- Reviewer role: independent, read-only. Write scope was `code-reviews/` only. No product, fixture, receipt, spec, test, playbook, or package file was modified; scratch work stayed inside the declared scratch path.
- Review scope per this step's authorized clarification: the **package dataflow** — `frontend/`, consumption of the digest-pinned receipt under `.agent-orch-inputs/dataforge/`, and the package export seam — plus every materialized package part. The legacy Python compatibility wrapper (`src/dashForge/_dataforge_compat.py`, `src/dashForge/generate.py`, `src/dashForge/main.py` and the tests exercising it) is out of scope here; it is already recorded as CMD-R1 from run `e40c272b0e04` with a DashForge backlog follow-up, and is deliberately not re-reported below.
- Verdict: **pass**. Five findings, all Low, none at or above the `severity_threshold: Medium` used by `review_verdict_clean`. Every mandated governance property holds and was re-derived here rather than inherited.

## Checks Run

Both mandated commands were executed by me in `/home/lee/projects/dashForge`, and their real exit codes are recorded in the canonical verdict JSON so `checks_run_match` can re-execute them:

- `python3 -m pytest tests/ -q` — exit **0**, output `10 passed in 4.32s`. This suite covers the legacy Python compatibility wrapper, which is out of scope for this package review; per the step mission, its exercising of that wrapper is not itself reported as a package finding. It is recorded here only because the step mandates running it and pinning its exit code.
- `npm --prefix frontend test -- src/core/data/SyntheticDataArtifactAdapter.test.ts src/features/export/meetingDashboardPackage.test.ts` — exit **0**, `Test Files 2 passed (2)`, `Tests 15 passed (15)` (11 synthetic-data adapter tests, 4 package-seam tests).

Preserved system-validator evidence (executed independently by the platform, summarized from the preserved record and **not** re-run — this step declares `allow_validator_rerun: False`):

- Validator 1 — `npm --prefix frontend test -- …` across five test files, for `validate_verified_work_package`: exit status `0`, passed, duration 1.856772s.
- Validator 2 — `npm --prefix frontend run build`, same step: exit status `0`, passed, duration 12.530106s.
- Validator 3 — `npm --prefix frontend run package:meeting-dashboard -- --smoke-result … --run-reference 31ad67eea2f1 --generated-at 2026-08-10T00:00:00.000Z`, for `materialize_meeting_dashboard_package` attempt 1: exit status `0`, passed, duration 0.886339s.

Nothing in this review contradicts those three records. All three passed on the first attempt in this run.

Read-only verification I performed myself (no result below is claimed as a gate result):

- Recomputed SHA-256 for all four files under `.agent-orch-inputs/dataforge/` against the receipt's `received_files` map — 4/4 hold — and for all **12** `file_hash_matches` pins declared in `playbooks/shakedown_client_meeting_dashboard_builder.yaml` — 12/12 hold. All four received files are mode `0444`.
- Compared each of the four vendored fixtures under `frontend/src/fixtures/dataforge/` to the corresponding received file by parsed-value equality: work package equals the receipt envelope's `payload` exactly, and snapshot, quality report, and reproduction manifest are each exactly equal to the received file.
- Enumerated every `sha256:` occurrence across all six package parts and tallied them by value.
- Recomputed the required material-surface set from `dashboard.spec.json` using the same rule as `collectRequiredMaterialSurfaceIds`, then diffed it against the claim ledger in both directions.
- Cross-checked part coherence: `binding-map.json.bindings` equals `dashboard.spec.json.dataContext.artifact.bindings`; `claim-ledger.json` equals `dashboard.spec.json.governance.claimLedger`; `dashboard-generation-result.json.output.dashboardSpec` equals `dashboard.spec.json`.
- Extracted and de-tagged the disclosure region of `dashboard.html`, counted rendered `standalone-claim-source` citations, enumerated every `<script>`, `<link>`, and absolute URL in the document, and searched it for hiding mechanisms (`sr-only`, `display:none`).
- Hashed `dashboard.html` against `artifacts/user-smoke/page-source.html`, and diffed the packaged `browser-smoke-result.json` against `artifacts/user-smoke/result.json` field by field.
- Static scan of `frontend/src/` and `frontend/package-tools/` for network primitives, credential and environment access, provider SDKs, and cross-repository imports.
- Executed `fs/promises.rm(<fresh empty dir>, { recursive: false, force: false })` under Node in scratch space to test the export seam's empty-output-directory branch directly.

## Lens Notes

**Cross-project isolation inside the scoped dataflow.** No boundary bleed exists in the package dataflow. The packaging pipeline is TypeScript end to end — `package:meeting-dashboard` is `vite build --ssr package-tools/materializeMeetingDashboardPackage.ts && node dist-package-tools/…`, with no Python step anywhere in it. Nothing under `frontend/src/` or `frontend/package-tools/` imports DataForge source, and nothing reads `.agent-orch-inputs/` at build or run time. DataForge enters only as four read-only JSON *data* fixtures under `frontend/src/fixtures/dataforge/`, adopted from the receipt — data, not code, and in-repo rather than cross-repository. I confirmed those fixtures have not drifted: each is exactly equal, by parsed value, to the corresponding received file that the receipt and the playbook pin byte-for-byte. Outside that fixture directory the only `dataForge` tokens under `frontend/src` are a provenance comment in `snowflakeCostIdleWarehouseWaste.ts`, a `"source": "dataForge"` label in preview data, and a test description string. This is the property the step asked me to verify for the scoped surface, and it holds cleanly.

**Digest identity across the package.** `sha256:57ed296635806a15deb7286879bae8b009fc551a86fce4b1f3cb564bcf775390` is the sole upstream artifact digest in the package and is identical in all four required places. Across the six parts it occurs 75 times; the only other digests present anywhere are the snapshot (`dbee5dc0…`, 3 occurrences) and the quality report (`92ae1d44…`, 2), both matching the received bytes. Specifically: `dashboard.spec.json` carries it at `dataContext.artifact.digest`, at `governance.claimLedger.upstreamArtifactDigest`, and on all 18 claim `source.artifactDigest` values; `binding-map.json` at `upstreamArtifactDigest`; `claim-ledger.json` at `upstreamArtifactDigest` and on all 18 claims; `dashboard-generation-result.json` at `inputReference.artifactDigest` and throughout its embedded spec. It also renders 14 times in `dashboard.html`, and matches the receipt's `canonical_artifact_digest`, the received envelope's own `digest` field, and `IDLE_WAREHOUSE_WORK_PACKAGE_DIGEST` in `verifiedSyntheticDataArtifacts.ts`. No competing or stale digest survives anywhere in the package.

**Chain of custody and its declared seam.** The receipt is candid that `materialize_artifact_input` refused this source run fail-closed for two stated reasons — the source `run.json` carries `employee_id: null` because the leg-1 launch did not export `AGENT_ORCH_EMPLOYEE_ID`, and the producer's `work-package.json` is not sealed into source-run evidence. Files were therefore operator-placed read-only, and custody rests on the playbook's byte pins rather than on a platform artifact-input attestation. I re-verified all 16 byte pins (12 playbook `file_hash_matches` plus 4 receipt `received_files`) independently; every one holds. What no in-repo mechanism does is *derive* the canonical digest: the plain file hash of `work-package.json` is `00768e3a…`, while `57ed2966…` is an Agent-Orch canonicalization asserted by the receipt and by the envelope. I attempted several plain-JSON canonicalizations of the payload and of `{envelope, payload}` and reproduced none of them, which is consistent with the digest being an Agent-Orch-specific canonical form rather than a naive serialization; no hashing primitive (`createHash`, `subtle.digest`, or equivalent) exists anywhere under `frontend/src` or `frontend/package-tools`. Recorded as PKG-R3 (Low).

**Uncited material surfaces cannot be ready.** The enforcement is layered and genuinely load-bearing. `collectRequiredMaterialSurfaceIds` derives the required set from the spec itself; `validateMaterialClaimCoverage` requires a claim per required surface, rejects duplicate claim ids, rejects duplicate surfaces, and rejects any claim citing a digest other than the manifest's; `createDashboardGenerationResult` folds those errors into `status`; and `createMeetingDashboardPackageParts` refuses to emit any file unless `status === "ready"` with zero errors. `validateClaimLedger` goes further at adapter time, requiring each claim's cited dataset, fields, and predicate to actually resolve against snapshot rows. I recomputed the required set independently from `dashboard.spec.json` — 5 widgets, `narrative:dashboard-description`, the 5 standard story-arc surfaces, `narrative:executive-summary`, and 6 `additionalMaterialSurfaceIds` (4 `metric:*`, 2 `recommendation:*`) — giving exactly 18. The ledger carries 18 claims across 18 distinct surfaces, all citing the correct digest, all with `dataset_observation` evidence. The set difference is empty in both directions, matching `validation.materialClaimCount: 18`, `claimCoverage: "passed"`, `status: "ready"`, `errors: []`. The one structural gap in the rule is `narrative.presenterNotes`, which ship inside the packaged spec but are never a required surface — recorded as PKG-R4 (Low). Separately, 12 of the 18 surfaces render a visible `standalone-claim-source` line; the five dashboard-stage widget cards do not — recorded as PKG-R5 (Low). Neither is a coverage violation: the ledger covers all 18.

**Browser provider credentials and direct model calls.** None exist in the scoped surface. A scan of `frontend/src` and `frontend/package-tools` for `fetch(`, `XMLHttpRequest`, `axios`, `WebSocket`, `process.env`, `import.meta.env`, `VITE_`, `api_key`, `x-api-key`, and provider names returns zero non-test hits. Runtime dependencies are only `ajv`, `echarts`, `react`, `react-dom`, and `react-grid-layout` — no provider SDK. The AI seam is `createStagedManifestGenerationClient`, which reads a staged JSON manifest out of a DOM element by id and revalidates it through `validateDashboardGenerationResult` — which re-derives the whole generation result deterministically and compares serializations, refusing anything not `ready`. Its provider label is literally `"Agent-Orch staged manifest"` and its model is `"staged-manifest"`. The only `Authorization`/`Bearer` strings in the tree are in tests asserting that live headers are *stripped* from serialized specs. The packaged `dashboard.html` carries exactly one `<script>` (a relative bundle reference), no credentials anywhere, and only two absolute URLs — both W3C SVG namespace URIs.

**Visible synthetic disclosure.** Present and genuinely visible. `dashboard.html` carries `data-testid="synthetic-quality-disclosure"` on a `role="status"` section with `data-quality-state="controlled"`, inside a root marked `data-demo="idle-warehouse-waste"` and `data-readiness="controlled"`. De-tagging that region yields the heading "Controlled synthetic-data quality", the bolded lead "Synthetic demo data", the full upstream sentence "All datasets in this work package are deterministic synthetic demo data; they are not observations from a live Snowflake account.", the controlled-fault tally "5 requested · 5 injected · 5 independently detected · 0 unexpected", the verified upstream digest in a `<code>` element, and the three upstream assumptions as list items. The document contains no `sr-only`, `display:none`, or `display: none` anywhere, so the disclosure is not suppressed even when the external stylesheet is unavailable. `meetingDashboardPackage.ts` independently gates six document markers — the scenario attribute, the disclosure test id, the controlled quality state, the literal text "Synthetic demo data", the spec's artifact digest, and the presence of both `standalone-claim-source` and "dataset observation" — and refuses to emit any package file if any marker is missing.

**Data mode and live-connector absence.** `dashboard.spec.json` has `dataContext.mode: "artifact"` and no live-connection key. `validateArtifactIdentity` cross-checks artifact type, payload schema version, employee id, and digest shape against the work package before any render; `validateArtifactFiles` cross-checks snapshot and quality-report paths and digests, snapshot pack/scenario/seed identity, and the quality report's scenario digest; `validateQuality` requires `passed` on both the work package and the quality report, agreement across requested/injected/detected fault counts, zero unexpected findings, and all story assertions passed. A grep across all six package parts for `authorization`, `bearer`, `password`, `token`, `apikey`, `secret`, `credential`, and `headers` returns nothing.

**Browser-smoke honesty.** `artifacts/user-smoke/result.json` reports `app_started`, `page_loaded`, `interaction_succeeded`, and `success_observed` all true, `blocking_errors: []`, and `observed_text: "Follow-up artifact is ready"`. The packaged `browser-smoke-result.json` is exactly equal to it as parsed values, with no key added or dropped — the only difference is key ordering from deterministic serialization. `start_exit_code: -15` is the expected SIGTERM teardown of the preview server, not a failure. The packaged `dashboard.html` is byte-identical (`62b82aed…`) to `artifacts/user-smoke/page-source.html`, so the document I reviewed is exactly what the browser rendered — an honest capture rather than a re-render. It is, however, a post-hydration DOM snapshot referencing four external `/assets/` paths that are not among the six package parts. Recorded as PKG-R2 (Low).

**Materialization determinism and re-runnability.** `stableJsonStringify` serializes with a sorted union of every key encountered in the value, giving key-order-stable output; `createDashboardGenerationResult` takes `runReference` and `generatedAt` as explicit parameters rather than reading the clock, so with pinned inputs the six parts are content-deterministic. `publishPackageFiles` writes into a `mkdtemp` staging directory and `rename`s it atomically, removing the staging directory in a `finally` block on any failure — a failed materialization therefore cannot publish a partial or stale package. Preserved validator 3 recorded exit status 0 for this run's materialization, and I did not re-run it (this step declares `allow_validator_rerun: False`), so my determinism statement rests on code reading plus that single preserved success, not on an independent reproduction. The defect I did reproduce is narrower: the branch intended to tolerate an existing *empty* output directory calls `rm(outputDirectory, { recursive: false, force: false })`, which throws `ERR_FS_EISDIR` even when the directory is empty. I ran exactly that call against a fresh empty directory in scratch space and got `Path is a directory: rm returned EISDIR (is a directory)`. The surrounding `catch` re-throws anything that is not `ENOENT`, so the command exits 1. Recorded as PKG-R1 (Low): fail-closed and loud, but the packaging command is not re-runnable once its output path exists in any form.

**Platform attestations handed to this step.** Read from the evaluator evidence packet rather than assumed. Route selection is recorded for 3 finished producer attempts and post-execution route attestation for 3 invocations, all `verified_true`, each with `actual_route` matching `resolved_route` (`codex_cli` for `validate_verified_work_package` and `materialize_meeting_dashboard_package`, `browser_smoke_runner` for `render_and_smoke_standalone`). Repository identity is `verified_true` for all 3, each classified `valid_root_git_directory` with an empty `differences` list. The evidence-chain snapshot taken before this evaluator was invoked reports `ok: true`, 4 entries checked, 62 artifacts checked, `first_divergence: null` — explicitly the evaluator-input snapshot, `terminal: false`, and not certified here as the approval-boundary or terminal attestation. Validator authority is `verified_true` for all three preserved records, under the stated policy that the independently executed validator record is authoritative. Per the temporal scope rule, each of these is bound to its own subject, phase, step, and invocation; I certify nothing about this step's own post-execution attestations, which are produced after this step returns. The one evidence class not available to me is `semantic_judge_execution`, correctly marked `not_applicable` because this playbook declares no `semantic_check` rule; no readiness criterion below depends on it.

## Findings Summary

| ID | Severity | Subject |
|----|----------|---------|
| PKG-R1 | Low | `publishPackageFiles` empty-output-directory branch always fails with `EISDIR`; the packaging command is not re-runnable |
| PKG-R2 | Low | `dashboard.html` is a DOM capture referencing four external `/assets/` paths that are not package parts |
| PKG-R3 | Low | The canonical upstream digest is never derived in-repo; it is a hardcoded constant backed only by external byte pins |
| PKG-R4 | Low | `narrative.presenterNotes` ship inside the packaged spec but are exempt from material-claim coverage |
| PKG-R5 | Low | The five dashboard-stage widget cards render values with no on-surface source citation |

No finding reaches the `Medium` threshold enforced by `review_verdict_clean`, and none of them undermines a mandated governance property.

## Readiness and Recommendation

All eleven readiness criteria are recorded `verified_true` in the canonical verdict JSON, each with the basis I derived myself. Every governance property the mission named holds: the scoped dataflow imports no DataForge source, no browser provider credential or direct model call exists in it, the upstream digest is identical across spec, bindings, claims, and generation result, a visible synthetic disclosure is present in `dashboard.html`, and no uncited material surface can reach `ready`.

**Recommendation: Ready for controlled pilot.**

Autonomous re-arm is not the right next step, and PKG-R1 is the specific reason. The packaging command fails with `EISDIR` the moment its output path exists — even empty — so an unattended re-run of this playbook against a populated `artifacts/current/client-meeting-dashboard-builder/` will burn an attempt on a defect that has nothing to do with the work. A controlled pilot, where an operator clears the output path before each run, carries none of that risk. PKG-R2 and PKG-R3 point the same way: the delivered HTML is not yet self-contained enough to hand to a client without the serving app, and the canonical digest is trusted rather than derived inside the product, so the operator-verified byte pins remain part of the control loop for now.
